{
  "name": "nextoffer",
  "version": "1.0.0",
  "description": "NextOffer - AI-Powered Placement Preparation Platform (MERN Stack)",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm.cmd run dev --prefix server\" \"npm.cmd run dev --prefix client\"",
    "client": "npm.cmd run dev --prefix client",
    "server": "npm.cmd run dev --prefix server",
    "install:all": "npm.cmd install && npm.cmd install --prefix server && npm.cmd install --prefix client",
    "seed": "npm.cmd run seed --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  },
  "author": "NextOffer Team",
  "license": "MIT"
}
